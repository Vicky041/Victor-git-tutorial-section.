#include "main.h"

int main(void)
{
int num1 = 4, num2 = 2;
printf("the sum %d and %d is : %d\n", num1, num2, add(num1, num2));
if (num1 != 7)
{
printf("not lucky number\n");
}else if (num1 % 2 ==
return (0);
}
